# MNIST Project

This folder does not include the training dataset. You can download it from the internet by searching for "MNIST dataset" and place it inside the `MNIST` folder with the name `data`.

The trained model `prvimodel.pth` is included and achieved 98% accuracy on the test set.
